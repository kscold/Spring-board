debug: false # logback ?? logging? ????? ?? ??? ???
management:
  endpoints:
    web:
      exposure:
        include: "*" # ????? ?????? ?? ??? ??


logging:
  level:
    org.example.boardproject: debug # ?? ???? ???
    org.springframework.web.servlet: debug # ? ??? ???? ?? ??? ??
    org.hibernate.type.descriptor.sql.BasicBinder: trace

spring:
  datasource: # mysql ??
    url: jdbc:mysql://localhost:3306/board
    username: kscold
    password: Tmdcks6502@
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    defer-datasource-initialization: true # ???? ??????? ??
    hibernate:
      ddl-auto: create # ???? ?? ???? ???? ????
#    open-in-view: false
    show-sql: true # sql ??? ???? ??
    properties: # ??? ??? ??
      hibernate:
        format_sql: true # ??? ????? ??? ???? ??? ??? ??
        default_batch_fetch_size: 100 # jpa?? ??? ???? ??? ???? ??? ??? ? ??? ??? ??? ? ??(n+1) ??? ??
  h2:
    console:
      enabled: true # ???? ??? ???? h2 DB? ???? ??
  sql:
    init:
      mode: always # data.sql? ?? ????? rule? ??? ??
#  thymeleaf:
#    cache: false

--- # yaml ??? ??? document ??? ? ??

spring:
  config:
    activate:
      on-cloud-platform: None
#  datasource:
#    url: jdbc:h2:mem:board;mode=mysql
#    driver-class-name: org.h2.Driver
#  sql:
#    init:
#      mode: always
#  test.database.replace: none
